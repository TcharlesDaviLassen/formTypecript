import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

// Ignora o que o TYPESCRIPT manda ! 
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
